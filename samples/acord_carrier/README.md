# Heritage National Group — ACORD P&C XML

- **Format:** XML with ACORD-style PascalCase elements
- **Quirks:** Mixed dates, `$` currency strings, `CurCd` attributes, extension elements, namespace on root only

## Files

- `sample_claims.xml` — 12 claims under `ClaimsSvcRq`
- `schema.xsd` — Simplified reference schema (not used by DiscoveryEngine)
